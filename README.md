# Proyecto Final — Análisis de Algoritmos

## Compilar

```powershell
g++ src/*.cpp -Isrc -o build/proyecto_completo.exe
```

## Ejecutar

```powershell
.\build\proyecto_completo.exe
```

El ejecutable busca los CSVs en la carpeta `data/`. Se corre desde la raíz del proyecto.
